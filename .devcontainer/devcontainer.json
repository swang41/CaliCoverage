{
  "name": "Python 3.13 Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "containerEnv": {
    "PIPENV_VENV_IN_PROJECT": "1"
  },
  "postCreateCommand": "pipenv install ipykernel && pipenv run python -m ipykernel install --user --name=my-pipenv-kernel --display-name='Pipenv Environment'",
  "postStartCommand": "git config --global --add safe.directory ${containerWorkspaceFolder}"
}
